version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt

# If your docs live in /docs (they do), no extra steps needed.
# If using myst-nb (notebooks), add jupyter + myst-nb to requirements.
