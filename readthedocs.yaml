version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py
  # Add this line to specify the source directory
  builder: html
  # Or use the explicit format:
  # fail_on_warning: false

python:
  install:
    - requirements: requirements.txt

# Explicitly set the source directory
sphinx:
  configuration: docs/conf.py
  # This tells Read the Docs where your source files are
  # Remove any custom build commands if you have them